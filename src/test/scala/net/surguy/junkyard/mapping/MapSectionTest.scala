package net.surguy.junkyard.mapping

import org.specs2.mutable.Specification


class MapSectionTest extends Specification {

/*
  val items = Robot("glub").at(5,5) :: Heap().at(3,4) :: Heap().at(3,3) :: PowerSocket().at(1,1) :: List()
  val map =  new MapSection(items)

  "checking a map for terrain" should {
     "find default terrain where not set" in { map.terrainAt(Coord(5,5)) must be equalTo(Glass()) }
     "find correct terrain where set" in { map.terrainAt(Coord(3,3)) must be equalTo(Heap()) }
  }
*/

}
