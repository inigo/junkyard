package net.surguy.junkyard.pathfinder

import net.surguy.junkyard._
import org.specs2.mutable.Specification

class AstarPathFinderTest extends Specification {
/*

  val items = Robot("glub").at(5,5) :: Heap().at(3,4) :: Heap().at(3,3) :: PowerSocket().at(1,1) :: Heap().at(3,1) :: Heap().at(4,1) :: Heap().at(3,5) :: List()
  val map = new MapSection(items)
  val pathRules = new JunkyardPathRules(map, new Robot("glob"))
  val pathFinder = new AstarPathFinder(pathRules)
*/

  /*
  "finding path" should {
    "take the correct first step" in { pathFinder.nextStep(Coord(5, 5), Coord(1,1)) must be equalTo(Some(Coord(4,5)))}
    "navigate past hills" in { pathFinder.nextStep(Coord(4, 2), Coord(1,1)) must be equalTo(Some(Coord(3,2)))}
  }*/

}